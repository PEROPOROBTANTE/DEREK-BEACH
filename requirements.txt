# Python dependencies for DEREK-BEACH project
# This file lists all external packages required for the project to run

# Core scientific computing
numpy>=1.21.0
pandas>=1.3.0
scipy>=1.7.0

# Network analysis
networkx>=2.6.0

# Natural Language Processing
spacy>=3.0.0
# Note: You may also need to download spaCy language models:
# python -m spacy download es_core_news_lg
# python -m spacy download es_core_news_sm

# Document processing
PyMuPDF>=1.18.0  # PDF processing (import as 'fitz')
PyPDF2>=1.26.0   # Alternative PDF library
python-docx>=0.8.11  # Word document processing

# Data validation and parsing
pydantic>=1.8.0
pyyaml>=5.4.0

# Fuzzy string matching
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.2  # Optional but recommended for better performance

# Visualization
pydot>=1.4.2
# Note: pydot requires Graphviz to be installed on your system
# Ubuntu/Debian: sudo apt-get install graphviz
# macOS: brew install graphviz
# Windows: Download from https://graphviz.org/download/
